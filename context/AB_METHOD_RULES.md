# AB Method Rules (from CLAUDE.md)

- You should always fallback to the `.ab-method/structure/index.yaml` (mapped to `context/AB_METHOD_STRUCTURE.md` or similar in this repo) to see where to read from files so always insist in the markdown files to look at that file first to know where to look and resume.
